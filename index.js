import { AppRegistry } from 'react-native';
import App from './App';

// const tempURL = process.env('WEBVIEW_URL')
console.log('[]][][ process: ', process)

AppRegistry.registerComponent('HelloWorld', () => App);
